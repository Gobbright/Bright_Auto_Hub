# Transparent car product images

These PNG files are foreground-only derivatives of the downloaded car catalogue images in `../cars/`.
The original source, creator and licence records remain in `../../IMAGE-ATTRIBUTION.json`.
Backgrounds were removed locally with the IMG.LY medium foreground-extraction model; the vehicle pixels were not generatively replaced.
